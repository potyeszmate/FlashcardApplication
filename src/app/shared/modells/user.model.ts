//User interface (What I wanted in the end: Get logged in USer's information + edit them in Profile Page - Didn't have time for it-)
export interface User {
    uid: string;
    email?: string;
    username?: string;
    displayName?: string;
    phone?: string;
    address?: string;
    photoURL?: string;
  }